# VB to VB.Net Migration

VS2008에서 지원한다.
VS2008및 SP1을 설치한 후 VB6의 vbp파일을 열어서 Migration을 진행한다.

LenB -> Len

string기본이 Unicode임

Drag 처리
http://www.vbtonet.com/conversion-standard-drag-and-drop/

Convert.ToUInt32(ColorTranslator.ToOle(System.Drawing.Color.Black))